<div class="ttt-board">
    {{#each controller.fields}}
        <div id="ttt-row-{{@index}}">
        {{#each this}}
            {{#if this}}
                <div id="ttt-column-{{@index}}" class="ttt-unclickable ttt-field">
                    {{this.symbol}}
                </div>
            {{else}}
                <div id="ttt-column-{{@index}}" class="ttt-field">
                </div>
            {{/if}}
        {{/each}}
        </div>
    {{/each}}
</div>